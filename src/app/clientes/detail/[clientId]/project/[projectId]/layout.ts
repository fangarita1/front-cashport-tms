import { Metadata } from "next";
import DetailClientPage from "./page";

export const metadata: Metadata = {
  title: "Detalle Facturas",
  description: "Detalle Facturas"
};
export default DetailClientPage;
