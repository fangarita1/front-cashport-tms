import { Metadata } from "next";
import VehicleInfoPage from "./page";

export const metadata: Metadata = {
  title: "Logistics",
  description: "Vehiculo info view"
};
export default VehicleInfoPage;
