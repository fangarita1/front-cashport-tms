import { Metadata } from "next";
import CreateGeneralPage from "./page";

export const metadata: Metadata = {
  title: "Materiales",
  description: "Vista general de materiales"
};
export default CreateGeneralPage;
